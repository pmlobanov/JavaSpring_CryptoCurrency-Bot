<configuration>
    <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Устанавливаем уровень логирования для наших пакетов -->
    <logger name="spbstu.mcs.telegramBot" level="DEBUG"/>

    <!-- Уменьшаем уровень логирования для Kafka, чтобы избежать спама -->
    <logger name="org.apache.kafka" level="WARN"/>

    <root level="INFO">
        <appender-ref ref="STDOUT" />
    </root>
</configuration>